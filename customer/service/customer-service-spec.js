describe('customerService', function() {

  beforeEach(module('customer'));

  it('should ...', inject(function(customerService) {

	//expect(customerService.doSomething()).toEqual('something');

  }));

});